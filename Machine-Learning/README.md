# 🤖 Machine Learning

Notes, lab notebooks, and references from my ML learning — covering Python data libraries, preprocessing, and a range of supervised and probabilistic algorithms. Most experiments here are from my ML lab using real datasets.

---

## What's Here

### 📦 python-libraries
Foundational Python libraries used throughout ML work.

| Folder | What's Inside |
|---|---|
| `numpy/` | Array operations, linear algebra, broadcasting |
| `pandas/` | DataFrames, data wrangling, aggregations |
| `matplotlib/` | Plotting, visualizations, chart customization |
| `scipy/` | Statistical functions, distributions, scientific computing |

### ⚙️ data-preprocessing
Cleaning, transforming, and splitting data before feeding it into models — missing values, encoding, scaling, train/test splits.

### 🧠 algorithms
Lab experiments implementing ML algorithms on real datasets.

| Folder | Algorithm | Notes |
|---|---|---|
| `knn/` | K-Nearest Neighbors | |
| `decision-tree/` | Decision Tree | |
| `naive-bayes/` | Naïve Bayes | |
| `logistic-regression/` | Logistic Regression | Telco Customer Churn dataset |
| `svm/` | Support Vector Machine | |
| `mlp/` | Multi-Layer Perceptron | |
| `random-forest/` | Random Forest | |
| `adaboost/` | AdaBoost | |
| `bbn/` | Bayesian Belief Network | |
| `hmm/` | Hidden Markov Model | |

---

## Datasets

Datasets used in these notebooks are not committed to the repo.

---

## Format

Most files here are Jupyter notebooks (`.ipynb`) from lab experiments. Some folders may also have short markdown notes alongside the notebook for concept summaries.
